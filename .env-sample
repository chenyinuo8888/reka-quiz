# Reka AI API Configuration
# Get your free API key at: https://link.reka.ai/free

# Primary API key (used both for video listing and chat)
API_KEY=your_api_key_here

# Endpoint serving your video catalog (must support POST /videos/get)
BASE_URL=https://vision-agent.api.reka.ai

# Optional: Override the chat endpoint (defaults to BASE_URL + /qa/chat)
# REKA_VIDEO_QA_ENDPOINT=https://vision-agent.api.reka.ai/qa/chat